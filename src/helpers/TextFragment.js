export default class TextFragment {
    constructor(x, y, text, fontSize) {
        this.positionLeft = x;
        this.positionTop = y;
        this.text = text;
        this.fontSizePt = fontSize;
        //fonstFace
        //fontColor
    }
};